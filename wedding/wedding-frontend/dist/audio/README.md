Place a licensed `wedding-song.mp3` file in this folder before deploying.
The path is referenced from `src/config/siteConfig.js` (`music.src`).
Browsers block autoplay-with-sound, so the player starts paused and requires a tap.
